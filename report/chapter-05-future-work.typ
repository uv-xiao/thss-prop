= 未来工作

== 未来工作总体目标

前文的现有工作展示了三条已经形成基础的研究线索：面向应用和架构的可编程接口能够降低领域定制门槛，面向硬件实现的前端抽象和综合优化能够提高 RTL 生成与硬件设计效率，大模型与形式化技术结合能够把策略生成、反馈修复和证据积累引入编译与硬件设计流程。但这些工作也共同暴露出更深层的剩余矛盾：软硬件协同的对象已经从单个模块、单条 pass 或单个内核扩展到架构接口、硬件结构、编译流程、运行时调度和产业级系统部署的联合空间，而现有工具链仍然缺少一个能够同时保持语义边界、生成能力、验证证据和运行反馈的系统化方法。

因此，未来工作不应被理解为若干已有项目的线性延伸，而应围绕“敏捷芯片设计与创新性编译技术驱动的软硬件协同”这一主题，进一步回答三个问题。第一，在架构、硬件和编译支持都需要快速生成与协同演化的情况下，如何让生成过程始终受验证约束，而不是退化为不可审计的自由生成。第二，当编译器成为连接应用语义、架构事实、硬件能力和运行时反馈的核心接口时，如何构建对人和智能体都可解释、可审计、可复现的编译基础设施。第三，在工业级异构架构上，如何把算子优化、编译决策、运行时任务图、数据移动和性能剖析反馈组织为长期演化的工程闭环，使单点优化技术能够落到复杂系统中。

围绕上述问题，本章提出三个相互支撑但各有侧重的未来方向：验证约束下的架构、硬件与编译协同生成；可解释、可审计的编译基础设施；面向工业级异构架构的算子优化与运行时编译协同。三者之间不是简单上下游关系。第一个方向关注生成对象的跨层一致性，第二个方向关注编译接口和证据组织方式，第三个方向关注产业级异构系统中的性能闭环。它们共同构成从方法、基础设施到场景落地的研究路线。

#figure(
  rect(width: 100%, inset: 10pt, stroke: gray + 0.7pt)[
    #table(
      columns: (1fr, 1fr, 1fr),
      inset: 6pt,
      align: center + horizon,
      table.header(
        [验证约束协同生成],
        [可审计编译基础设施],
        [异构算子与运行时协同],
      ),
      [语义边界、类型化产物、可执行 oracle],
      [目标事实、显式 IR、语义契约、证据记忆],
      [算子优化、任务图、性能剖析、运行时反馈],
      [架构/硬件/编译共同演化],
      [人和智能体共同使用的编译接口],
      [工业级异构架构中的长期优化闭环],
    )
  ],
  caption: [未来工作三方向的关系。三者不是工具名称并列，而分别对应跨层生成方法、编译证据基础设施和工业异构场景中的性能闭环。],
)

== 验证约束下的架构、硬件与编译协同生成

第一项未来工作面向架构、硬件与编译支持的协同生成。第二章中的 APS/Aquas 已经说明，若应用接口、架构描述、编译 IR 和硬件生成流程能够被统一组织，领域定制处理器和近数据处理系统就可以从“手工拼接工具链”转向“由接口驱动的端到端协同”。第四章中的 OriGen 则说明，大模型可以在 RTL 生成中利用编译反馈 进行修复和迭代，但其对象仍主要停留在局部 RTL 代码和编译错误反馈。下一步需要把这两类思路结合起来：让大模型进入架构、硬件和编译的联合生成过程，但让语义边界、类型化产物和可执行验证始终作为权威约束。

该方向的核心判断是，软硬件协同生成不能把自然语言需求直接映射为不可分解的 RTL 或编译器代码，而应先建立明确的语义边界。该边界把人的需求、算法意图、架构约束和智能体提案转化为类型化产物，例如接口描述、任务/执行主体语义、硬件阶段性表示、编译降低计划、验证义务和执行轨迹。自然语言可以帮助解释需求、提出候选和组织修复，但不能成为系统事实本身；系统事实必须由可解析、可执行、可重放的对象承载。

在具体方法上，未来工作将研究阶段化硬件生成与阶段化编译降低的协同框架。硬件侧不一次性生成完整实现，而是从架构接口、并发语义、通信协议和存储结构开始，逐步进入微架构、RTL 和综合约束；编译侧也不直接生成目标后端，而是从程序语义、目标事实、IR 降低、编译过程和运行时 ABI 逐步推进。两个过程通过共同的可执行 oracle 和跨层检查对齐：同一个任务或算子应当能够在高层语义模型、硬件原型、编译产物和运行时执行之间产生可比较的执行轨迹；若执行轨迹不一致，系统应能定位责任边界，例如接口语义错误、降低错误、调度错误或硬件实现错误。

这种方法也是对 Spine 原型思路的系统化提升。已有基础已经包含 Delta 执行主体/任务设计界面、生成器驱动执行、执行轨迹流、案例报告和严格 GCD Delta-to-RTL 验证路径等要素，说明小规模语义 oracle、任务执行和 RTL 对齐可以被组织为可重放证据。未来研究需要进一步扩大该机制的覆盖面：一方面，使架构描述能够表达 APS/Aquas 类系统中的处理器-加速器接口、存储/缓存约束和领域专用操作；另一方面，使智能体能够在有界行为空间中提出接口修订、硬件结构候选、编译降低候选和修复建议，并由类型检查、仿真、等价性检查、综合反馈和运行轨迹共同筛选。

预期贡献不是单纯“用大模型生成硬件”，而是提出一套验证约束下的协同生成方法。其关键科学问题包括：如何定义跨层语义边界，使架构、硬件和编译对象可以共同引用同一组事实；如何设计类型化产物，使智能体修改具有明确所有权、输入输出和验证责任；如何组织可执行 oracle，使高层语义、硬件实现和编译产物可以持续对齐；以及如何将反例和执行轨迹转化为可复用的修复知识。若该方向能够成立，敏捷芯片设计将不再只依赖更快的单点生成器，而可以依赖一个带有语义、证据和反馈的协同生成系统。

== 可解释、可审计的编译基础设施

第二项未来工作面向可解释、可审计的编译基础设施。第一章已经指出，编译与架构是衔接软硬件领域的重要接口；第二、三、四章进一步说明，无论是 APS/Aquas 的端到端处理器生成、ISAMORE 的分阶段等式饱和与定制指令复用、SkyEgg 的等价图高层综合优化，还是 EggMind 的 EqSatL 策略合成，真正困难的部分都不是孤立编译过程的实现，而是如何让目标事实、IR 表达、语义约束、变换过程、后端产物和评价证据保持一致。大模型和智能体方法进入这一过程后，编译基础设施更不能只是“可运行”的脚本集合，而必须成为可解释、可审计、可复现的智能体协作媒介。

该方向将以“目标事实 -> 显式 IR 与语义契约 -> 受约束变换 -> 可执行后端产物 -> 证据与反馈”为基本链条。目标事实包括并行层级、存储层级、通信与同步约束、指令语义、布局限制、运行时 ABI 和性能剖析能力；显式 IR 负责承载程序结构与变换边界；语义契约负责说明操作、区域、方言和后端产物的含义；受约束变换负责把分析、重写、编译过程、检查门、语义执行和智能体行为统一到可记录的编译器行为机制中；后端产物包括可执行代码、调度计划、内核、运行时配置和验证脚本；证据与反馈则包括 TraceDB 中的事实/事件、重写证据、检查门结果、测试输出、性能剖析数据和人工审阅记录。

IntelliC 的未来研究重点正是构建这样一类编译基础设施。其基础设计将 IR 拆分为 $S_y + S_e$：$S_y$ 负责语法、结构、身份、验证、规范文本和解析，$S_e$ 负责由类型化语义定义、语义层级键和 TraceDB 承载的语义定义。这样的拆分避免把语义隐藏在解析器或编译过程的临时状态中，也避免把智能体的解释当作编译事实。一个操作可以有具体值、抽象范围、符号、后端证据等多个语义层级；同一个编译流程可以通过 TraceDB 记录匹配、修改意图、语义事实、诊断、义务和证据链接，从而让人和智能体都能追踪“为什么发生了这个变换、它依赖哪些事实、通过了哪些检查门、产生了哪些后端证据”。

在智能体参与方面，编译基础设施需要明确区分固定行为、智能体行为和由智能体演化出的固定行为。固定行为可以是传统编译过程、验证检查门或后端交接；智能体行为可以在受限输入、受限输出和明确证据模式下提出候选变换、候选编译流程或诊断解释；由智能体演化出的固定行为则需要经过测试、语义检查和审计后，才被固化为可复用的流程组件。这样的边界是必要的，因为编译器中的错误往往具有跨层传播性：一个看似局部的重写可能破坏布局假设、别名关系、同步语义或后端 ABI。若没有 TraceDB、检查门和修改意图等显式证据，智能体的“合理解释”很难转化为可信编译行为。

该方向与前一方向互补。验证约束下的协同生成需要架构/硬件目标描述、可执行 oracle 和跨层执行轨迹；可解释编译基础设施则提供 IR、语义、编译器行为、编译过程、后端交接和证据记忆。换言之，前一方向回答“生成对象如何跨架构、硬件和编译保持一致”，本方向回答“编译层如何把这些对象变成可检查、可演化、可被智能体使用的基础设施”。预期成果包括一个最小可执行编译器纵切：从 Python 构造界面生成 MLIR/xDSL 风格 IR，经过结构验证、语义执行、规范化、重写/检查门、后端证据和 TraceDB 审计，并用包含循环携带语义的非平凡例子证明该链条不是玩具级直线程序。

== 面向工业级异构架构的算子优化与运行时编译协同

第三项未来工作面向工业级异构架构的算子优化与运行时编译协同。它不是前两个方向的简单应用，而是一个独立的产业级压力测试：当目标从单个可控硬件原型扩展到真实异构系统时，编译器不仅要生成高性能内核，还要面对动态图捕获、运行时调度、数据移动、多设备通信、服务延迟、吞吐、性能剖析和回归稳定性等问题。PyTorch `torch.compile`/TorchDynamo 已经把动态图程序、图断点、后端编译和即时执行回退组织为编译-运行时边界 @pytorchCompile2026；TensorRT-LLM 则展示了 LLM 推理系统中引擎构建、内核融合、量化、KV 缓存、连续批处理、分页注意力和服务后端的共同作用 @tensorrtllm2026。这些系统说明，工业级异构架构上的优化对象天然跨越编译与运行时。

本方向采用并列的两条子线。第一条子线研究工业级异构架构上的芯片算子自动优化。其目标不是只自动生成单个算子实现，而是构建面向复杂融合算子、Torch 训练/推理核心算子和长程优化任务的智能体协作框架。该框架的输入应包括芯片架构描述、ISA 与存储层级、编译约束、算子语义和性能目标；其行为空间应覆盖候选实现生成、IR 重写、编译过程组合、性能剖析调用、形式化辅助建模、回归测试和经验记忆更新；其输出不只是一段内核代码，而应包括正确性证据、性能证据、适用配置、失败案例和可复用优化经验。

NVIDIA AVO 是这一方向的重要外部证据，但不应被理解为狭义基线。AVO 说明，在 Blackwell B200 GPU 注意力内核优化中，自主代码智能体可以结合谱系、领域知识和执行反馈作为变异算子，进入性能敏感的内核/编译优化流程 @avo2026。这证明“智能体 + 评价器 + 执行反馈”在工业硬件附近是技术上真实的趋势。然而，本报告拟研究的问题更宽：目标算子不局限于注意力内核，目标硬件不局限于单一 GPU，优化过程不应停留在代码变异，而需要结合目标 IR、编译过程、性能剖析轨迹、形式化约束、回归验证和长期经验记忆。也就是说，AVO 支撑方向正确性，但剩余问题仍是如何把智能体搜索嵌入可维护、可审计、可迁移的编译和算子优化基础设施。

第二条子线研究运行时分布式协同。PTO Runtime 的层级运行时原型提供了重要基础：系统用 L0--L6 层级对应 Ascend NPU 集群拓扑，其中 L2 是芯片边界，L3 以上由编排器、调度器和执行单元递归组合；编排器通过 TensorMap 从 TaskArgs 的输入、输出和读写标签推导依赖，Ring 提供带反压的槽位池，调度器负责连接队列、就绪队列和完成队列，工作线程在线程或进程模式下把 Callable、TaskArgs 和 CallConfig 分发给下一级执行单元。L2 芯片级运行时又包含主机运行时、AICPU 调度器和 AICore 执行单元的三程序模型，形成从主机编排到设备任务执行的明确边界。

基于这一基础，未来工作需要把运行时协同从“可执行任务图”推进到“可被编译器和智能体使用的运行时证据系统”。一方面，TensorMap、RingBuffer、就绪/完成协议、任务组、CallConfig 和 mailbox/blob ABI 可以扩展为描述数据依赖、设备边界、执行配置和任务完成状态的结构化对象；另一方面，性能剖析、张量转储、PMU、运行时轨迹和失败报告应进入统一证据层，反馈给算子优化和编译决策。这样，运行时不只是被动执行编译产物，而能向编译器暴露数据移动瓶颈、任务粒度失衡、设备边界开销、存储层级压力和调度等待原因。

两条子线在“工业级异构架构”处汇合，但并不强行写成单向闭环。算子自动优化需要运行时提供真实反馈和部署约束，运行时分布式协同也需要编译器提供任务图、内核、布局和配置决策；二者共同面向的是跨硬件架构的软硬件协同生态。未来研究将优先选择能够被自动验证和重复评估的核心场景，例如 Torch 训练算子、LLM 推理融合算子、多设备任务图和运行时调度案例，通过正确性测试、性能剖析、轨迹重放、回归基准测试和人工审计共同评价。其预期贡献是把单点内核优化、编译过程组合和运行时调度反馈提升为可长期演化的系统能力。

== 集成路线、里程碑与风险控制

上述三个方向需要分阶段推进，以避免一开始就落入过大的系统范围。第一阶段聚焦验证约束下协同生成的小型闭环，选择语义明确、接口清晰、验证成本可控的硬件案例，完成 oracle、IR、RTL/代码生成和执行轨迹对齐的端到端验证。该阶段的目标不是覆盖复杂设计，而是证明语义边界、类型化产物、阶段化生成和可执行 oracle 能够共同工作，并形成可重放证据。

第二阶段聚焦可解释编译基础设施的最小可执行纵切。该阶段应完成语法/语义分离的核心对象模型、规范 IR 解析/打印、TraceDB、语义执行、固定行为、重写/检查门和后端证据的基本链条，并用包含循环、区域、循环携带值和至少一种优化变换的例子检验基础设施深度。该阶段的评价重点是可解释性和可审计性：每一次变换都应能够追溯依赖事实、语义义务、修改意图、检查门结果和最终证据。

第三阶段聚焦工业级异构架构中的算子优化与运行时协同。一方面，推进复杂融合算子和 Torch 训练/推理算子的长程自动优化智能体协作框架，形成候选生成、验证、性能剖析、回归和经验记忆循环；另一方面，推进 PTO Runtime 分布式特性的任务图协同、数据移动、就绪/完成协议和可观测性建设，使运行时轨迹能够反馈到编译和优化决策。该阶段的核心评价不只是单次性能提升，而是多轮迭代的稳定性、可维护性、跨配置迁移能力和错误恢复能力。

第四阶段进行跨层案例整合。理想的综合案例应同时包含架构/硬件约束、编译 IR 与 pass、算子或任务图优化、运行时执行和 trace 反馈，使前三个方向能够在同一系统中相互验证。该阶段应产出论文、开源工具链、可复现实验和文档化证据，强调研究方法而非只展示工程功能。

主要风险有四类。第一，语义边界过大，导致系统难以实现和验证；应从小闭环开始，先证明一个窄而深的路径，再逐步扩展产物类型。第二，智能体生成不可控，产生不可审计或不可维护的修改；应限制智能体行为的输入输出，使用类型化产物、TraceDB、检查门和重放约束其行为。第三，运行时研究依赖真实硬件和复杂部署环境；应同时保留仿真、单元基准测试、轨迹重放和真实硬件验证路径，避免评价完全被硬件可用性阻塞。第四，评估指标不统一，导致正确性、性能、可维护性和研究贡献彼此脱节；应为每个阶段明确正确性、性能、证据质量、可复现性和人工审计成本等多维指标，并把失败案例纳入证据库。
