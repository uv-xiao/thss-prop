= 未来工作

== 未来工作总体目标

前文已有工作展示了三类研究基础。第二章说明，应用需求可以通过架构接口、定制指令、微架构机制和加速器候选选择进入领域定制体系；第三章说明，架构能力只有经过硬件前端、IR、调度、综合和验证约束，才能成为质量可控的实现；第四章说明，大模型与智能体方法若要进入硬件设计和编译优化，就必须依附于形式化对象、编译基础设施和可执行检查，而不能停留在自由文本生成。三类基础共同指向一个更完整的博士阶段研究目标：构建由架构接口、硬件实现、编译接口和智能体能力共同支撑的跨硬件架构软硬件协同方法。

未来工作不应被理解为已有系统的简单延长。APS/Aquas、ISAMORE、Cayman、Cement、Clay、SkyEgg、OriGen 和 EggMind 已经分别回答了“如何形成架构能力”“如何实现高质量硬件”“如何约束智能体优化”的局部问题，但博士论文仍需要进一步回答这些局部能力如何组合成一个系统性研究命题。换言之，后续研究要把应用意图、架构接口、硬件生成、编译降低、目标执行环境、测试基准和智能体行为放到同一套可检查对象中，使敏捷芯片设计与创新性编译技术能够相互定义、相互约束并共同演化。

基于这一判断，后续研究分为三个方向。第一，研究验证约束下的架构、硬件与编译协同生成，回答生成对象如何在架构、硬件和编译层保持一致。第二，研究智能体编译基础设施，回答编译层如何为人和智能体提供可操作、可检查、可演化的基础对象。第三，研究面向工业级异构架构的算子优化与智能体化编译协同，回答上述方法如何进入真实异构系统中的算子、任务图、数据移动和性能优化问题。三个方向不是线性上下游关系，而是从协同生成方法、编译基础设施和产业级复杂场景三个侧面共同补全本报告的主线。

#figure(
  rect(width: 100%, inset: 10pt, stroke: gray + 0.7pt)[
    #table(
      columns: (1fr, 1fr, 1fr),
      inset: 6pt,
      align: center + horizon,
      table.header(
        [验证约束协同生成],
        [智能体编译基础设施],
        [异构算子与智能体编译],
      ),
      [语义边界、类型化产物、可执行判定基准],
      [目标事实、显式 IR、语义契约、证据记忆],
      [算子优化、任务图、性能剖析、执行轨迹],
      [架构、硬件与编译对象保持一致],
      [编译器成为人和智能体共同使用的基础设施],
      [工业级异构架构中的长期优化能力],
    )
  ],
  caption: [未来工作三方向的关系。三者分别从协同生成方法、编译基础设施和工业异构场景补全本报告的软硬件协同主线。],
)

== 验证约束下的架构、硬件与编译协同生成

=== 问题来源与研究问题

第一项未来工作面向架构、硬件与编译支持的协同生成。第二章中的 APS/Aquas 已经说明，若应用接口、架构描述、编译 IR 和硬件生成流程能够被统一组织，领域定制处理器和近数据处理系统就可以从手工拼接工具链转向由接口驱动的端到端协同。第四章引言中讨论的 OriGen 则说明，大模型可以利用编译错误等工具信息修复 RTL，但其对象仍主要停留在局部代码和局部反馈。后续研究需要把这两类经验统一起来：大模型和智能体可以参与架构、硬件和编译的候选生成，但系统事实必须由可解析、可执行、可重放的对象承载。

该方向的核心研究问题是：在架构描述、硬件实现和编译支持同时快速演化的情况下，如何定义一组跨层语义边界，使智能体提出的修改能够被类型化产物、可执行判定基准、仿真、等价性检查和综合结果共同约束。这里的“生成”不是自然语言到 RTL 的一次性映射，而是把人的应用意图、目标架构能力、硬件阶段性表示、编译降低计划和验证义务组织成一组相互引用的对象。只有这些对象之间可以持续相互校验，敏捷芯片设计才不会退化为不可检查的自动代码生成。

=== 研究方法与系统设计

未来研究将采用阶段化协同生成方法。硬件侧不直接生成完整实现，而是从架构接口、并发语义、通信协议和存储结构开始，逐步进入微架构、RTL 和综合约束；编译侧也不直接生成目标后端，而是从程序语义、目标事实、IR 降低、编译过程和系统 ABI 逐步推进。两个过程通过共同的可执行判定基准对齐：同一个任务或算子应当能够在高层语义模型、硬件原型、编译产物和目标执行环境之间产生可比较的执行轨迹；若执行轨迹不一致，系统应能定位责任边界，例如接口语义错误、降低错误、调度错误或硬件实现错误。

Spine 原型可以作为这一方向的重要基础，但它不能只被理解为 OriGen 的延伸。更准确地说，Spine 是 APS/Aquas 式端到端协同框架的智能体化发展：应用意图进入架构接口，架构接口约束硬件生成，硬件生成结果需要编译降低和执行判定基准共同检查，智能体在其中提出有限范围内的接口修订、硬件结构候选、编译降低候选和修复建议。已有 Delta 执行主体/任务设计界面、生成器驱动执行、执行轨迹流、案例报告和 GCD Delta-to-RTL 验证路径，说明小规模语义判定基准、任务执行和 RTL 对齐可以被组织为可重放证据。后续研究需要扩大覆盖面，使架构描述能够表达处理器-加速器接口、存储/缓存约束和领域专用操作，并使智能体行为始终受类型检查、仿真、等价性检查、综合结果和基准测试约束。

=== 对全局故事的补全意义

这一方向补全的是“敏捷芯片设计”侧的系统能力。前文已有工作已经分别展示了可编程接口、可复用定制指令、微架构感知综合和形式化策略生成，但仍缺少把架构、硬件和编译共同生成、共同检查的统一方法。如果该方向能够成立，敏捷芯片设计将不再只依赖更快的硬件生成器，而是依赖一套带有语义边界、类型化产物、可执行判定基准和跨层检查的协同生成体系。这一体系也为第二、三项未来工作提供对象来源：智能体编译基础设施需要承载这些生成对象，工业级异构算子优化需要在真实目标约束中使用这些对象。

== 智能体编译基础设施

=== 问题来源与研究问题

第二项未来工作面向智能体编译基础设施。第一章已经指出，编译接口是衔接软硬件领域的重要接口；第二、三、四章进一步说明，无论是 APS/Aquas 的端到端处理器生成、ISAMORE 的分阶段等价饱和与定制指令复用、SkyEgg 的等价图高层综合优化，还是 EggMind 的 EqSatL 策略合成，真正困难的部分都不是孤立编译过程的实现，而是如何让目标事实、IR 表达、语义约束、变换过程、后端产物和评价证据保持一致。大模型和智能体方法进入这一过程后，编译基础设施更不能只是可运行脚本集合，而必须成为智能体能力提升的结构化媒介。

该方向的核心研究问题是：如何把编译器中的事实、语义、变换、检查和产物组织成智能体可以安全操作的对象。传统编译器往往把大量语义隐藏在 pass 实现、临时分析结果或后端约定中，人类专家可以凭经验理解这些隐含关系，但智能体若直接修改代码或编译流程，容易破坏布局假设、别名关系、同步语义、目标 ABI 或后端约束。因此，编译基础设施需要显式暴露目标事实、IR 结构、语义契约、修改意图、检查门、后端证据和历史运行记录，使智能体行为有输入边界、输出形态和检查依据。

=== 研究方法与系统设计

未来研究将以“目标事实 -> 显式 IR 与语义契约 -> 受约束变换 -> 可执行后端产物 -> 证据与反馈”为基本链条。目标事实包括并行层级、存储层级、通信与同步约束、指令语义、布局限制、系统 ABI 和性能剖析能力；显式 IR 负责承载程序结构与变换边界；语义契约说明操作、区域、方言和后端产物的含义；受约束变换把分析、重写、编译过程、检查门、语义执行和智能体行为统一到可记录的编译器行为机制中；后端产物包括可执行代码、调度计划、计算内核、系统配置和验证脚本；证据与反馈包括 TraceDB 中的事实/事件、重写证据、检查门结果、测试输出、性能剖析数据和人工审阅记录。

IntelliC 的研究重点正是构建这样一类基础设施。其基础设计将 IR 拆分为 $S_y + S_e$：$S_y$ 负责语法、结构、身份、验证、规范文本和解析，$S_e$ 负责由类型化语义定义、语义层级键和 TraceDB 承载的语义定义。这样的拆分避免把语义隐藏在解析器或编译过程的临时状态中，也避免把智能体的解释当作编译事实。一个操作可以有具体值、抽象范围、符号、后端证据等多个语义层级；同一个编译流程可以通过 TraceDB 记录匹配、修改意图、语义事实、诊断、义务和证据链接，从而让人和智能体都能追踪某次变换依赖哪些事实、通过哪些检查、产生哪些后端证据。

在智能体参与方面，编译基础设施需要区分三类行为。第一类是固定编译器行为，例如传统编译过程、验证检查或后端交接；第二类是智能体行为，例如在受限输入、受限输出和明确证据模式下提出候选变换、候选流程或诊断解释；第三类是经过检验后可以固化的行为，即智能体提出的可复用流程组件在测试、语义检查和人工审阅后被转化为固定编译器能力。这样的边界使智能体不是绕过编译器，而是通过编译器原生对象参与优化。

=== 对全局故事的补全意义

这一方向补全的是“创新性编译技术”侧的系统能力。第一项未来工作需要能够承载架构、硬件和编译生成对象的编译层；第三项未来工作需要能够记录目标事实、优化动作和性能证据的编译层。智能体编译基础设施正是二者之间的中介：它把已有等价图、MLIR、EqSatL、TraceDB、语义契约和后端证据组织成智能体可用的编译接口。由此，编译器不再只是硬件能力的调用工具，而成为软硬件协同中组织事实、限制行动、积累经验和推动智能体能力提升的基础设施。

== 面向工业级异构架构的算子优化与智能体化编译协同

=== 问题来源与研究问题

第三项未来工作面向工业级异构架构的算子优化与智能体化编译协同。它不是前两个方向的简单应用，而是一个独立的复杂场景：当目标从单个可控硬件原型扩展到真实异构系统时，编译器不仅要生成高性能计算内核，还要面对动态图捕获、任务调度、数据移动、多设备通信、服务延迟、吞吐、性能剖析和回归稳定性等问题。PyTorch @pytorchCompile2026 `torch.compile`/TorchDynamo 已经把动态图程序、图断点、后端编译和即时执行回退组织为编译与动态执行之间的边界；TensorRT-LLM @tensorrtllm2026 则展示了大模型推理系统中引擎构建、内核融合、量化、KV 缓存、连续批处理、分页注意力和服务后端的共同作用。

与此同时，NVIDIA GB200 NVL72 @nvidiaGB200NVL72、CloudMatrix384 @cloudmatrix3842025 和 UB-Mesh @ubmesh2025 等系统把优化对象推进到机架级互连、跨芯片通信和真实服务拓扑，使算子优化必须与通信路径、数据移动和系统边界共同考虑。NVIDIA AVO @avo2026 进一步说明，在 Blackwell B200 GPU 注意力内核优化中，自主代码智能体可以结合谱系、领域知识和执行结果作为变异算子，进入性能敏感的内核/编译优化流程。这些证据共同支撑一个判断：工业级异构架构上的优化对象天然跨越编译、硬件拓扑和执行系统，智能体方法必须围绕目标事实、性能剖析、测试基准和长期经验组织行动。

=== 研究方法与系统设计

本方向采用并列的两条子线。第一条子线研究工业级异构架构上的芯片算子自动优化。其目标不是只自动生成单个算子实现，而是构建面向复杂融合算子、Torch 训练/推理核心算子和长程优化任务的智能体协作框架。该框架的输入包括芯片架构描述、ISA 与存储层级、编译约束、算子语义和性能目标；行为空间覆盖候选实现生成、IR 重写、编译过程组合、性能剖析调用、形式化辅助建模、回归测试和经验记忆更新；输出不只是一段计算内核代码，而应包括正确性证据、性能证据、适用配置、失败案例和可复用优化经验。

AVO 在这里是重要趋势证据，而不是狭义基线。它说明“智能体 + 评价器 + 执行结果”在工业硬件附近是技术上真实的方向，但本报告拟研究的问题更宽：目标算子不局限于注意力内核，目标硬件不局限于单一 GPU，优化过程不应停留在代码变异，而需要结合目标 IR、编译过程、性能剖析轨迹、形式化约束、回归验证和长期经验记忆。也就是说，AVO 支撑方向合理性，剩余科学问题是如何把智能体搜索嵌入可维护、可检查、可迁移的编译和算子优化基础设施。

第二条子线研究分布式执行系统协同。PTO 层级执行原型提供了重要基础：系统用 L0--L6 层级对应 Ascend NPU 集群拓扑，其中 L2 是芯片边界，L3 以上由编排器、调度器和执行单元递归组合；编排器通过 TensorMap 从 TaskArgs 的输入、输出和读写标签推导依赖，Ring 提供带反压的槽位池，调度器负责连接队列、就绪队列和完成队列，工作线程在线程或进程模式下把 Callable、TaskArgs 和 CallConfig 分发给下一级执行单元。L2 芯片级执行系统又包含主机侧程序、AICPU 调度器和 AICore 执行单元的三程序模型，形成从主机编排到设备任务执行的明确边界。

基于这一基础，后续研究需要把分布式执行系统从可执行任务图推进到可被编译器和智能体使用的执行证据系统。一方面，TensorMap、RingBuffer、就绪/完成协议、任务组、CallConfig 和 mailbox/blob ABI 可以扩展为描述数据依赖、设备边界、执行配置和任务完成状态的结构化对象；另一方面，性能剖析、张量转储、PMU、执行轨迹和失败报告应进入统一证据层，反馈给算子优化和编译决策。这样，执行系统不只是被动执行编译产物，而能向编译器暴露数据移动瓶颈、任务粒度失衡、设备边界开销、存储层级压力和调度等待原因。

=== 对全局故事的补全意义

两条子线在“工业级异构架构”处汇合，但不应被写成单向关系。算子自动优化需要执行系统提供真实反馈和部署约束，分布式执行系统也需要编译器提供任务图、内核、布局和配置决策；二者共同面向的是跨硬件架构的软硬件协同生态。未来研究将优先选择能够被自动验证和重复评估的核心场景，例如 Torch 训练算子、LLM 推理融合算子、多设备任务图和调度案例，通过正确性测试、性能剖析、轨迹重放、回归基准测试和人工检查共同评价。

这一方向补全的是报告主线的产业级复杂性。第二章到第四章主要建立方法与基础设施，第三项未来工作则检验这些方法能否面对真实异构系统中的长程优化问题。若该方向成立，单点内核优化、编译过程组合和执行调度信息将不再是彼此分离的工程细节，而会成为可长期演化的系统能力，从而把“敏捷芯片设计与创新性编译技术驱动的软硬件协同”落到复杂产业场景。

== 本章小结

本章提出的三项未来工作分别补全全局故事中的三个缺口。验证约束下的架构、硬件与编译协同生成，使应用意图、架构接口、硬件生成和编译降低能够在共同语义边界下相互校验；智能体编译基础设施，使编译器成为人和智能体共同使用的事实、语义、变换和证据组织系统；面向工业级异构架构的算子优化与智能体化编译协同，则把上述方法推进到真实多芯片、多层执行系统和长程性能优化场景中。

这三个方向共同服务于博士论文的核心目标：不是单独提高某个生成器、综合器或优化器的能力，而是研究如何在跨硬件架构的软硬件协同生态中形成可复用架构能力、可控硬件实现、可演化编译接口和受约束智能体能力。它们与第二至第四章的已有工作形成连续关系，也使后续研究能够围绕明确的科学问题、方法对象和验证方式展开。
