= 大模型与形式化技术驱动的软硬件协同方法

== 引言

前两章分别讨论了领域能力如何进入架构接口，以及这些能力如何落到高质量硬件实现。本章进一步讨论一个新的方法层问题：当大模型和智能体方法开始进入代码生成、硬件生成、编译优化和工程自动化流程时，如何使它们真正服务软硬件协同，而不是退化为难以检查的文本生成。第一章已经指出，LLM 与智能体方法是一条重要技术趋势，但芯片设计与编译优化对语义正确性、接口协议、时序、资源、验证和长期维护提出了远高于一般代码补全的要求。因此，本章的重点不是证明“大模型能写硬件或编译器”，而是说明大模型能力必须通过形式化对象、编译基础设施、验证反馈和证据记忆进入协同流程。

OriGen @origen2024 是这一问题的一个重要背景。它面向开放 RTL 代码生成，指出商业 LLM 在软件代码生成上已经表现出较强能力，也能生成一定质量的 RTL，但闭源模型带来隐私、安全、可定制性和研究可复现问题；开源代码模型虽然便于部署和微调，却受到高质量 RTL 数据不足的限制。硬件设计中的 RTL 往往包含 IP 和专有设计知识，不能简单依赖商业模型远程生成；同时，RTL 语法、时序、综合风格和验证要求又明显不同于普通软件代码，通用代码数据难以充分覆盖。

OriGen 的启示不在于把 RTL 生成作为本章重点展开，而在于说明大模型硬件生成必须进入工具约束流程。它通过代码到代码数据增强提升 RTL 数据质量，并构建 VerilogFixEval 评测模型基于编译错误信息修复 RTL 的能力。训练数据中包含自然语言指令、错误代码、编译错误信息和修正代码，使开源模型学习从工具反馈到代码修复的映射。对本文而言，这表明硬件生成中的 LLM 能力必须与编译、语法检查、仿真和修复样例结合，后续面向验证约束的架构、硬件与编译协同生成也需要进一步把这种约束扩展到接口协议、等价性检查、综合结果和跨层执行轨迹。

本章以 EggMind 为重点，讨论大模型能力如何通过 EqSatL、等价图（e-graph）、证明派生 motif 记忆和可处理性引导进入形式化优化框架。OriGen 在引言中提供大模型硬件生成侧的背景，EggMind 则作为本章完整展开的现有工作，展示大模型与形式化技术结合后如何把策略搜索、证据组织和智能体化迭代纳入可检查的工具链对象。

#figure(
  image("assets/eggmind/overview.pdf", width: 94%),
  caption: [EggMind 总体流程。离线阶段通过智能体工作流合成 EqSatL 策略，在线阶段由后端将策略降低为可执行 EqSat 代码并复用到新优化任务。],
)

== 等价图策略自动化

=== 研究背景与设计目标

EggMind 直接回应了第一章和第三章共同提出的编译接口问题：当等价图、EqSat 和基于重写的优化已经成为硬件综合、编译优化和 ISA 定制的重要方法，如何进一步自动化这些优化过程中的策略设计。等价饱和的优势在于用等价图紧凑表示大量语义等价程序，避免过早承诺某条重写序列；ISAMORE 和 SkyEgg 分别利用这一思想发现可复用定制指令、联合探索硬件变换/映射/调度空间。但 EqSat 的实用瓶颈同样明显：重写空间一旦变大，等价图会快速膨胀，执行时间和内存成为主要限制，最终结果很大程度上取决于规则如何分组、何时应用、应用多少轮、何时简化。

因此，EggMind @eggmind2026 把自动化目标从“让 LLM 直接写 egglog 代码”转向“合成可复用 EqSat 策略产物”。在 EggMind 中，策略指组织 EqSat 搜索的控制结构，包括规则集划分、饱和调度、等价图简化和预算控制。这个对象位于重写规则和后端执行之间：它不改变语义规则本身，但决定哪些规则可以互相作用，怎样分阶段暴露优化机会，以及如何在搜索过程中收缩冗余状态。这样，LLM 不再直接操作庞大的原始等价图或低层 egglog 细节，而是围绕一个可检查、可复用、可评估的策略表示进行搜索。

=== 可检查的策略表示

EggMind @eggmind2026 的第一个核心抽象是 EqSatL。EqSatL 是表示 EqSat 策略的 DSL，目标是在紧凑性、形式化验证和表达能力之间取得平衡。它把策略设计分解为三个控制面。第一是规则集划分，用语义标签将重写词表分为具有含义的规则集，而不是暴露冗长规则列表，使 LLM 能在更高层级表达“哪些规则应当一起或分开作用”。第二是调度构造，用流树组织阶段、序列和 `repeat` 区域，使规则交互可以被分阶段、可重复但有界地执行。第三是简化控制，把 `simplify` 显式化为一等控制节点，通过强度参数和可选 LLM 提示决定何时、如何收缩等价图，避免无约束增长。

#figure(
  image("assets/eggmind/eqsatl.pdf", width: 86%),
  caption: [EqSatL 策略示例。策略表示将规则集划分、阶段化调度、repeat 结构和 simplify 控制显式化，便于大模型生成、工具验证和后端执行。],
)

这一设计的关键在于，EqSatL 将战略意图与后端实现解耦。传统原始 egglog 脚本把策略、规则应用细节和执行机制混在一起，LLM 需要反复修补低层代码，反馈也难以定位责任边界。EqSatL 则把策略产物放在稳定边界上：它足够紧凑，便于 LLM 生成和修改；它足够显式，便于检查划分、调度和简化是否合理；它也足够有表达力，能控制规则互动、重复搜索和状态收缩。对软硬件协同而言，这种产物边界非常重要，因为受约束自动化必须操作稳定对象，而不是依赖一段难以检查的低层脚本。

=== 智能体工作流：离线策略合成与在线复用

EggMind @eggmind2026 的第二个核心是智能体工作流。它把离线策略合成组织为 Strategist、Generator、Evaluator、Partitioner 和 Simplifier 的受控循环。工作流记忆中维护策略仓库，包括当前最佳策略、有前景策略和待评估策略；每轮迭代中，Strategist 根据当前证据选择动作，Generator 产生或修改 EqSatL 策略，Evaluator 以不同预算执行 EqSat 并返回质量、时间和内存反馈，Partitioner 与 Simplifier 则提供规则集划分和简化提示。评估后，系统更新最佳/有前景/待评估状态，并把成功运行中的证据写回记忆。这样，智能体工作流不依赖开放式对话历史，而依赖明确的策略表示、评估结果和可复用证据。

从系统结构看，EggMind 将策略发现分为离线合成和在线复用两个阶段。离线阶段以一组演化案例、重写规则和代价模型为输入，由智能体工作流生成 EqSatL 策略产物；在线阶段则把选定策略应用到新案例，由后端将 EqSatL 降低为可执行 EqSat 代码并运行。这个分离具有重要工程意义：昂贵的策略探索成本在离线阶段支付，并通过可复用策略摊销到后续优化任务；在线优化阶段不需要让 LLM 反复进入执行路径，从而降低不可控性和运行开销。对博士开题报告而言，这一点说明 EggMind 不是一般意义的“LLM 调参”，而是一种把策略搜索、执行评价和证据记忆分层组织的编译自动化系统。

=== 证明驱动的经验记忆

第三个核心是证明派生重写 motif 缓存。EqSat 运行若产生更优结果，后端可以给出初始表达式到最终表达式的等价证明；但原始证明树通常庞大、包含大量同余/传递性记录，不适合作为 LLM 上下文。EggMind @eggmind2026 因此从证明树中提取局部重写 motif，将具体规则链提升为语义标签链，例如某类向量提升后接某类向量优化。motif 缓存根据相对代价降低和跨案例频率保留高价值 motifs，并在后续策略生成中提供紧凑结构证据。这样，系统记忆的不是模糊经验描述，而是来自成功证明路径的可复用重写交互。

这一机制解决了智能体编译优化中的一个关键问题：经验如何不变成难以检查的自然语言偏好。EggMind 首先定位证明树中的局部证明区域，用同余路径标记子表达式上下文；随后把局部重写链通过规则到标签映射提升为语义 tag chain，并对重复 motif 进行去重和排序。motif 的评分同时考虑相对收益和跨案例频率，因此缓存保留的是在成功优化中反复出现、且能带来实际代价降低的结构证据。后续 Generator 或 Strategist 使用这些 motif 时，看到的是来自等价证明的结构化引导，而不是未经验证的人工经验。

#block(breakable: false)[
  #rect(width: 100%, inset: 7pt, stroke: gray + 0.7pt)[
    #text(size: 7.6pt)[
      EggMind 的证明派生记忆用相对收益更新 motif 评分。对候选案例 $c$ 和 motif $gamma$，源文中的核心更新可写为：
      $
        g(c) = max(0, 1 - "final-cost"(c) / "previous-best-cost"(c))
      $
      $
        "score"(gamma) <- (1 - alpha) "score"(gamma) + alpha g(c)
      $
      $
        "best-gain"(gamma) <- max("best-gain"(gamma), g(c))
      $
    ]
  ]
]

#figure(
  image("assets/eggmind/proof-memory.pdf", width: 82%),
  caption: [EggMind 证明派生 motif 记忆。系统从等价证明中抽取局部重写 motif，并将其转化为可复用、可检查的策略证据。],
)

=== 可处理性引导与简化提示

第四个核心是可处理性引导。EggMind @eggmind2026 用基于依赖的规则集风险模型为规则集划分提供稳定性指导：它把重写词表建模为规则依赖图，根据 RHS/LHS 激活、算子重叠和子树可匹配性估计规则之间的互相激活风险；划分目标奖励前向阶段依赖，惩罚回流、首阶段流入和同阶段纠缠，从而使规则交互更接近稳定的前向激活。与此同时，LLM 引导简化提示为不同阶段提供偏好/剪除模式对，将简化从单一全局代价剪枝变成阶段局部结构偏好。二者共同服务一个目标：让 LLM 搜索停留在稳定、可执行、可复用的策略空间中，而不是不断触发等价图膨胀。

具体地，基于依赖的规则集风险模型将重写规则作为图节点，用有向边表示一条规则可能启发另一条规则的强度；阶段分配的目标是鼓励规则依赖从前一阶段流向后一阶段，同时抑制后向激活、首阶段反复被后续规则激活，以及同阶段规则强耦合。LLM 引导简化提示则在每个阶段给出少量偏好/剪除模式对，形成局部简化偏序，并通过有界惩罚改变 e-class 内候选的保留优先级。这种做法没有把 LLM 提示作为硬删除规则，而是让评价继续检验提示是否过强或损害后续优化；如果提示不合适，后续迭代可以修正。因此，EggMind 对 LLM 的使用始终围绕可执行评价和可回退控制，而不是让模型直接决定最终正确性。

#block(breakable: false)[
  #rect(width: 100%, inset: 7pt, stroke: gray + 0.7pt)[
    #text(size: 7.2pt)[
      规则集风险模型把重写词表表示为有向依赖图 $G = (R, E, w)$，并为阶段分配 $phi: R -> {1, dots, k}$ 求解如下目标：
      $
        max_phi
        alpha sum_((i,j) in E) w_(i,j) "1"[phi(i) + 1 = phi(j)]
        - beta sum_((i,j) in E) w_(i,j) "1"[phi(i) > phi(j)]
      $
      $
        - gamma sum_((i,j) in E) w_(i,j) "1"[phi(j) = 1, phi(i) != 1]
        - delta sum_((i,j) in E) w_(i,j) "1"[phi(i) = phi(j)]
      $
      四项分别奖励前向依赖，并惩罚回流、首阶段流入和同阶段纠缠。对 LLM 引导简化，阶段 $t$ 中的偏好/剪除模式对定义局部偏序：
      $
        (p^+, p^-) in Pi_t times Pi_t, quad p^- prec_t p^+
      $
      简化评分以基础代价加有界提示惩罚表示：
      $
        s(e) = c(e) + p_"llm"(e)
      $
      其中 $theta$ 控制按评分剪除的强度。
    ]
  ]
]

=== 实验评估与对软硬件协同的意义

EggMind @eggmind2026 的评估说明，这种“LLM + 形式化策略表示 + 验证约束体系”的组合确实改善了 EqSat 优化。向量化基准测试上，EggMind 相对完整 EqSat 将最终代价几何平均降低 45.1%，相对专家调优 Isaria 策略降低 20.6%；在线执行相对 Isaria 获得 2.21x 几何平均加速，并相对完整 EqSat 将峰值内存几何平均降低 69.1%。消融实验显示，motif 缓存、`repeat` 结构、Strategist 和 Partitioner 均对质量或稳定性有贡献，尤其缺少 motif 缓存或 repeat 会显著恶化代价、执行时间和内存。

#figure(
  image("assets/eggmind/isaria-main-comparison.pdf", width: 92%),
  caption: [EggMind 在 Isaria/vectorization 基准上的主要结果。该结果支持正文关于策略质量、执行时间和内存占用改善的论断。],
)

EggMind @eggmind2026 还展示了跨领域迁移能力。在 XLA 张量编译器基准测试中，合成策略在 17 个案例中 13 个获得低于无引导基线的最终代价，其余 4 个持平，同时相对完整 EqSat 获得 11.89x 几何平均执行时间加速。在 EqMap 逻辑综合案例研究中，EggMind 使用增强重写空间，在 carry-chain 基准测试上取得最好的质量效率权衡，相对原始规则 $i=4$ 和 $i=5$ 分别降低 33.76% 和 8.75% 代价，并相对无引导高预算搜索降低 51.94% 峰值内存。这些结果说明，EggMind 不是为单个向量化基准测试手工调参，而是在不同等价图优化域中学习和复用策略组织方式。

从本报告主线看，EggMind 是 ISAMORE 和 SkyEgg 的策略层延伸。ISAMORE 依赖等价图反合一和分阶段 EqSat 发现可复用定制指令，SkyEgg 依赖等价图表达硬件变换与映射选择；二者都面临重写空间如何组织、搜索如何收敛、策略如何迁移的问题。EggMind 的贡献是把这些策略从隐含经验提升为显式、可检查、可复用的 EqSatL 产物，并用智能体工作流、证明派生 motif 记忆和可处理性引导对 LLM 搜索施加结构化约束。这也是本报告中“大模型与形式化技术结合”的核心含义：LLM 负责提出和改进策略，形式化对象负责限定可操作边界，证明和评价器负责提供证据，编译基础设施负责执行和检查。

== 本章小结

本章回应第一章提出的第三个科学问题：编译接口如何组织程序语义、硬件能力和智能体行为，形成跨硬件架构的可验证、可复用编译协同机制。EggMind 展示了在编译优化和等价图搜索中，LLM 应当通过 EqSatL、智能体工作流、证明派生 motif 记忆和可处理性引导等结构化对象进入流程；OriGen 作为背景说明 LLM 进入 RTL 生成时必须面对数据质量、编译反馈和自反思能力问题。二者共同说明，大模型方法的价值不在于绕过硬件工具链，而在于帮助工具链进行策略生成、错误修复、证据积累和长程优化。

这也为第五章未来工作奠定了方法基础。后续研究需要把 OriGen 所体现的反馈式硬件生成背景、EggMind 所体现的可检查策略生成，以及第二、三章中的架构接口和硬件综合机制结合起来，进一步构建验证约束下的架构、硬件与编译协同生成，智能体编译基础设施，以及面向工业级异构架构的算子优化与智能体化编译协同。换言之，未来工作不是让 agent 自由生成设计，而是让 agent 在明确的架构接口、IR 对象、形式化约束、测试/基准测试和执行轨迹中进行受控协作。
