= 面向高质量硬件实现的前端抽象与综合优化

== 引言

第二章讨论了领域能力如何被识别、表达并接入架构接口，但架构能力本身并不等于高质量硬件实现。一个自定义指令、加速器 region 或硬件前端描述，只有在时序、面积、资源、接口协议、控制逻辑和验证条件都满足目标约束时，才能真正成为可部署的系统能力。因此，本章围绕第一章提出的第二个核心科学问题展开：架构能力如何高质量落到可综合、可验证、性能与资源可控的硬件实现。

这一问题的难点在于硬件实现质量来自多个层次的共同决定。RTL 能提供细粒度控制，但抽象层次低、生产率不足；HLS 能从软件描述生成硬件，却常把关键微架构决策隐藏在 pragma 和启发式调度之后；硬件 DSL 与 IR 试图提高表达能力和可分析性，但如果不能携带时序、资源和映射信息，仍难以形成可预测结果。因此，本章按 HECTOR、Cement、Clay、SkyEgg 的顺序组织：HECTOR 作为早期多层 IR 与硬件综合基础设施积累，提供后续工作的技术背景；Cement 关注周期确定型硬件行为表达；Clay 关注面向 ASIP 的微架构感知前端与综合机制；SkyEgg 则把代数变换、硬件映射和调度放入统一 e-graph 设计空间，形成对高质量硬件实现问题的进一步推进。

== 多层硬件综合基础设施积累

=== 问题定位与技术框架

HECTOR 是本人较早参与的硬件综合基础设施工作，其意义在于把硬件综合问题从单一 HLS 工具实现，推进到基于多层 IR 的方法学表达。传统 HLS 工具、硬件生成器和领域专用 DSL 往往各自实现一套前端、IR、调度、生成和优化流程，工程成本高，方法难复用。HECTOR 提出基于 MLIR 的两层 IR：高层 IR 将 computation 与带 timing information 的 control graph 绑定，低层 IR 则用于描述硬件模块和 elastic interconnections，并可进一步转换为 synthesizable RTL @hector2022。

HECTOR 对本报告的作用不在于作为最重要的代表性工作展开，而在于提供了后续研究反复依赖的基础设施视角。它说明硬件综合可以被组织成多层 IR、转换 pass、调度信息和 RTL lowering 的组合，而不是只能依赖黑盒 HLS 或手写 RTL。基于这一思想，后续 Cement、Clay、SkyEgg 等工作都可以被理解为在不同维度上深化 HECTOR 开启的问题：Cement 强调前端语言必须显式表达周期行为和 timing constraints；Clay 强调 ASIP 指令扩展的 coupling strategy 和 microarchitectural attributes 需要进入综合约束；SkyEgg 则强调 algebraic transformation、mapping 和 scheduling 不应被拆成固定顺序的阶段。

=== 对后续工作的支撑

因此，HECTOR 在本文中承担“综合基础设施积累”的角色。它为本章后续讨论提供两个重要判断。第一，硬件实现质量需要 IR 层支撑，只有把控制、时序、模块连接和 lowering 边界显式化，后续方法才有可分析对象。第二，多层 IR 本身还不够，IR 中携带哪些时序、资源、接口和映射信息，决定了工具能优化到什么程度。Cement、Clay 和 SkyEgg 分别从前端时序表达、微架构感知综合和联合优化三个方向回答这一问题。

== 周期确定型硬件前端

=== 研究背景与设计目标

Cement 面向 FPGA 加速器设计中的一个长期矛盾：设计者需要硬件级别的微架构表达和时序控制，但 RTL 生产率低、HLS 结果难预测、许多 DSL 又只覆盖特定结构。HDL 能直接描述连接和寄存器传输，却缺少对跨周期行为的显式表达，复杂 FSM 和流水线控制需要大量手工设计；HLS 从 C/C++ 等软件语言出发，依赖 pragma 与启发式调度补足微架构信息，导致同一设计在不同工具版本或配置下可能产生不可预测结果；部分 DSL 提升了安全性或特定并行模式表达，但往往牺牲一般微架构能力。Cement 因此提出 cycle-deterministic eHDL CmtHDL 与 CmtC 编译器，目标是在不牺牲微架构表达力的前提下，提高硬件前端生产率和结果可预测性 @cement2024。

#figure(
  kind: table,
  placement: none,
  text(size: 8pt)[
    #table(
      columns: (0.8fr, 1.15fr, 1.35fr, 1.55fr, 0.75fr, 0.75fr),
      inset: 2.2pt,
      align: horizon,
      table.header([框架], [入口/产物], [控制逻辑], [微架构表达], [周期确定], [时序感知]),
      [Chisel], [Scala eHDL / RTL], [人工描述], [一般表达，无预设协议], [是], [否],
      [BSV + Stmt], [HDL / RTL], [人工 + 过程式语句], [一般表达，带 ready-enable 协议], [否], [否],
      [Filament], [HDL / Calyx], [时间线类型], [受限静态流水], [是], [是],
      [HLS 工具], [C/C++ 等 / RTL], [软件程序 + 指令], [受工具指令和预设接口限制], [否], [是],
      [Dahlia], [DSL / HLS 或 Calyx], [软件程序 + 仿射类型], [顺序控制器和固定接口], [否], [是],
      [Spatial], [DSL / RTL], [软件程序 + 模板], [受模板和固定接口限制], [否], [是],
      [Aetherling], [DSL / RTL], [空间-时间类型], [静态流式结构], [是], [是],
      [Calyx], [IR / RTL], [人工 + 过程式语句], [受 go-done 协议限制], [部分], [部分],
      [Cement], [Rust eHDL / RTL], [人工 + 过程式语句], [一般表达，无预设协议], [是], [是],
    )
  ],
  caption: [Cement 与代表性 FPGA 编程框架的对比。该表对应源文 Table 1，重点说明 Cement 同时保留一般微架构表达、过程式控制描述、周期确定性和时序感知。],
)

=== 周期行为表达与控制子语言

CmtHDL 嵌入 Rust，先利用 Rust trait 和宏系统定义 data types、bundles、interfaces、module instantiation、operations 和 port connections，再引入 event layer 与 control sub-language。event layer 用 event 表示一组带 guard 的硬件行为，并显式记录这些行为发生的 cycle sequence；control sub-language 用 `seq`、`par`、`if`、`for`、`while`、`step` 等 procedural statements 描述 event 的时序关系，使设计者能够直接表达确定的跨周期行为。与普通 RTL 只描述“每周期连接如何生效”不同，CmtHDL 允许设计者把“哪些行为在哪些周期发生”作为一等对象写入前端表示 @cement2024。

#figure(
  image("assets/cement/shuffler.pdf", width: 76%),
  caption: [Cement 源文 Figure 1 的流水 shuffler 示例。该图同时展示四级流水 shuffler 的微架构，以及 Chisel、HLS 和 CmtHDL 对同一结构的描述方式。],
)

这一抽象对复杂控制逻辑尤其重要。以 shuffler 和 arbiter pipeline 为例，HDL 设计者需要手工保证发送、仲裁、接收和 resend 之间的周期对齐；HLS 版本则可能因为软件循环依赖模型和工具启发式调度而无法稳定达到 II=1。CmtHDL 可以用 `seq` statement 明确流水线不同 stage 的 timing，并由 CmtC 检查 timing violation。这种 cycle determinism 不是简单固定延迟，而是在静态或动态输入条件下保证 event 的执行周期可推导、可检查、可综合，从而降低手工 FSM 和流水线控制中的隐性错误。

#figure(
  kind: table,
  placement: none,
  table(
    columns: (0.85fr, 1.35fr, 2.5fr, 1.4fr),
    inset: 5pt,
    align: horizon,
    table.header([语句], [宏语法], [时序规则], [延迟含义]),
    [`step`], [`[x] e0, e1 [y]`], [等待入口事件 `x`，在同一周期触发 `e0` 与 `e1`，再等待出口事件 `y`。], [1 个执行周期，加入口/出口等待周期],
    [`seq`], [`seq { s0; s1 }`], [无额外间隔地顺序触发 `s0` 与 `s1`。], [`L(s0) + L(s1)`],
    [`par`], [`par { s0; s1 }`], [立即并行触发 `s0` 与 `s1`，等待二者都完成。], [`max(L(s0), L(s1))`],
    [`if`], [`if { [cond] t_stmt else e_stmt }`], [根据条件事件是否发生，立即触发 then 或 else 分支。], [所选分支延迟],
    [`for`], [`for { [i in range] do_stmt }`], [按静态范围无间隔重复执行循环体。], [`L(do_stmt) * trip-count`],
    [`while`], [`while { [cond] do_stmt }`], [在条件成立时无间隔重复执行循环体，直到条件失败。], [`L(do_stmt) * trip-count`],
  ),
  caption: [Cement 控制子语言语句。该表对应源文 Table 3，说明 `step`、`seq`、`par`、`if`、`for`、`while` 如何把过程式控制转换为可推导的周期行为。],
)

=== 周期分析与有限状态机综合

CmtC 的核心编译能力包括 timing analysis 和 FSM synthesis。Timing analysis 分为 static analysis 和 dynamic monitoring：前者对 elaboration-time 可确定的 static events/statements 推导 cycle sequence，在仿真前发现数据收发周期不匹配；后者在仿真中插入 monitor signals 和 assertions，用于捕获数据相关行为下的 timing violations。FSM synthesis 则从 control sub-language 生成严格满足时序规格的控制逻辑，并用 state tree representation、transition table 和编码优化减少 FF/LUT 开销。这使 Cement 不只是前端语法改进，而是在语言、分析和综合之间形成闭环：设计者显式写出时序，编译器检查时序并生成资源优化的控制电路 @cement2024。

具体而言，static timing analysis 首先识别 cycle sequence 可在 elaboration time 确定的 static events/statements，然后自底向上推导 statement latency，再自顶向下推导每个 event 的执行周期，并比较每个 port/wire 的发送与接收周期。如果发送周期与接收周期不一致，系统可以在仿真前报告 invalid-data 或 data-loss 风险。dynamic monitoring 则把 event 是否在当前周期发生编码为 boolean signals，为端口收发生成监测信号并插入 SystemVerilog assertions，从而处理数据相关控制流下的时序违规。FSM synthesis 使用 state tree representation 表达 control sub-language 的状态空间：叶节点对应状态，边携带状态编码片段，transition table 描述输入条件与下一状态；随后通过编码优化在 FF 数、LUT 数和解码延迟之间折中。这些机制使 CmtHDL 的“周期确定性”落到编译器可检查、可综合、可优化的具体对象上。

#figure(
  image("assets/cement/state-tree.png", width: 78%),
  caption: [Cement 有限状态机综合中的状态树表示。控制子语言被转化为可编码、可优化的状态空间，而不是由设计者手写控制器。],
)

=== 实验评估与意义

实验上，Cement 在 PolyBench、systolic array 和 sparse accelerator 等案例中展示了周期确定前端的实际价值。相对 Vitis HLS，Cement 在未流水和流水配置下分别获得 1.41x 和 1.52x 几何平均加速，并平均节省 23%/47% LUT 和 68%/78% FF；相对 Dahlia-Calyx flow，Cement 获得 3.49x 几何平均加速，并节省 54% LUT 和 82% FF。Systolic array case study 中，CmtHDL 通过显式周期对齐降低开发成本，并相对 EMS 节省 49% LUT 和 23% DSP，频率提升 7%，throughput per DSP 提升 33%。Sparse accelerator case study 中，CmtHDL 用更少代码描述 shuffler，并相对 Vitis HLS 方案提升频率、显著节省 LUT/FF。这些结果说明，硬件前端的价值不仅在于“写得更快”，也在于使时序行为可预测、可检查、可综合优化 @cement2024。

== 面向实现质量的前端抽象与综合机制

=== 研究背景与设计目标

Clay 与 Cement 不是版本递进关系，而是面向不同硬件实现问题的互补工作。Cement 关注 FPGA 设计前端如何显式表达周期行为和控制逻辑；Clay 则回到 ASIP 和自定义指令场景，关注一条指令扩展如何在不同处理器微架构、不同耦合策略和不同寄存器/访存路径下获得灵活、高质量实现。RISC-V 生态降低了指令扩展实验门槛，但现有 ASIP 框架往往把指令描述、耦合位置和微架构约束绑定在特定处理器模板中，使“能写出一条自定义指令”和“能把它高质量接入不同微架构”之间仍存在明显距离 @clay2025。

这一距离首先体现在流水线内耦合的限制上。流水线内耦合能够复用主处理器已有控制和数据通路，适合组合逻辑型指令扩展，因此许多工具优先选择这种方式；但它也会把自定义指令限制在处理器流水线已有资源和时序边界内。Clay 源文指出，这类方法难以支持任意状态化行为和复杂控制流，循环往往需要常数循环次数或提前展开；同时，寄存器文件端口数、访存通道和执行阶段位置等约束也会限制一条指令中可表达的寄存器与内存交互 @clay2025。对于机器学习、信号处理和流式数据处理中的指令扩展，这些限制会直接影响设计者能否把多次访存、循环和私有状态封装成一个可复用架构能力。

Clay 的研究目标正是把这些原本隐含在处理器模板中的接口事实显式化。它提出一个高层 ASIP 框架，以统一指令扩展接口连接 CADL 前端和微架构感知综合流程。设计者在 CADL 中描述指令编码、私有状态和无时序行为语义；工具再根据目标处理器提供的耦合策略、接口动作、阶段范围、延迟和资源限制，自动选择流水线内耦合或协处理器耦合，并生成对应硬件实现 @clay2025。这个设计把第二章讨论的“架构接口”进一步推进到实现层：同一个指令语义是否适合放进流水线、是否需要协处理器、每次寄存器/访存动作应该排在哪个阶段，并不是后端局部选择，而是影响指令扩展可移植性和性能的核心综合问题。

#figure(
  image("assets/clay/framework.pdf", width: 78%),
  caption: [Clay 的 CADL 前端、指令流、综合流程和目标处理器生成关系。图中展示了自定义状态、接口原语、指令流、阶段划分、流水线调度和微架构生成之间的连接。],
)

=== 统一指令扩展接口与架构描述语言

Clay 的关键抽象是统一指令扩展接口。源文把不同处理器耦合机制抽象成与微架构无关的接口原语，例如寄存器读请求/响应、寄存器写请求、内存读请求/响应、内存写请求和程序计数器更新；每个接口原语再由目标微架构给出延迟、可放置阶段范围和并发资源上限等属性 @clay2025。这样，流水线内耦合和协处理器耦合不再是两套完全不同的前端目标，而是同一指令语义在不同接口属性下的不同实现候选。对综合器而言，接口原语描述“需要发生什么接口行为”，微架构属性描述“这些行为何时、何地、以多少资源发生才合法”。

#table(
  columns: (1fr, 2.1fr, 2.2fr),
  align: horizon,
  table.header([接口对象], [技术含义], [在综合中的作用]),
  [接口原语], [寄存器读写、访存读写、程序计数器更新等指令与处理器交互对象], [形成调度对象，使指令行为能在流水线或协处理器接口上实现],
  [阶段范围], [一个接口原语可放入的处理器流水线阶段区间], [约束流水线内耦合时的合法放置位置],
  [延迟], [请求、响应或计算动作之间的时序关系], [参与依赖约束和状态机/流水线调度],
  [资源上限], [同一周期可并发使用的端口、通道或功能资源数量], [限制寄存器读写、访存和功能单元使用，决定调度可行性],
)

CADL 则在这个接口之上提供面向设计者的行为描述语言。它包含指令编码、私有状态和行为三部分：编码描述指令格式和译码逻辑，私有状态可以是自定义寄存器或 scratchpad memory，行为部分用接口操作和高层控制流描述自定义指令语义 @clay2025。重要的是，CADL 中的行为是无时序的，设计者不需要在前端手写请求-响应握手、流水线阶段或协处理器状态机；这些时序细节由后续综合流程根据目标微架构属性推导。与此同时，CADL 保留了 `if`、`for`、`while` 等控制流，循环边界可以来自解码变量或控制寄存器，因此能够表达传统流水线内自定义指令工具难以支持的状态化行为和变长循环。

在编译流程中，CADL 先被转换为 HIR。HIR 包含指令识别/译码逻辑和对应行为的控制数据流图，随后经过条件转换、用户指定循环展开等变换，进入面向调度和硬件生成的后续表示 @clay2025。这个层次划分的意义在于，Clay 并没有把“指令是什么”和“指令如何接入某个处理器”混在同一个描述中，而是先保留高层语义，再把目标处理器事实作为综合约束逐步引入。由此，CADL 既能保持 ASIP 前端的可写性，又能为后端生成微架构相关实现留下足够信息。

=== 微架构感知综合与调度

Clay 的综合流程以自定义指令集合和候选耦合策略为输入。对于每条指令，系统按优先级尝试候选策略，通常先尝试开销较低的流水线内耦合，再尝试表达能力更强的协处理器耦合；每次尝试前先进行合法性检查，例如多基本块的状态化指令不能直接作为流水线内耦合实现，而需要程序计数器更新的指令也不能放入普通协处理器接口 @clay2025。通过这种先筛选、再调度、再生成的流程，Clay 把“选择哪类接口”从人工经验变成可由工具检查和搜索的综合步骤。

调度是 Clay 实现质量的核心。源文将微架构属性转化为整数线性规划中的约束：每个接口原语有延迟和可放置阶段范围，每类资源有并发上限，每个操作消耗相应资源；请求-响应式接口原语的依赖关系由请求延迟和响应动作共同约束 @clay2025。对于不含复杂控制流的指令，综合器进行普通调度；对于状态化、多基本块指令，综合器先对基本块调度，再生成有限状态机；对于协处理器中包含循环的指令，内层循环可以采用 modulo scheduling，并逐步增大启动间隔直到找到合法调度。这样，Clay 可以在同一框架内覆盖简单组合指令、状态化控制流、复杂访存和协处理器长延迟行为。

#figure(
  image("assets/clay/schedule.png", width: 78%),
  caption: [Clay 调度示例。源文通过不同访存端口和资源约束下的调度展示微架构属性如何影响启动间隔、延迟和最终硬件实现。],
)

`stream_add` 示例清楚体现了这种微架构感知的必要性。该指令包含循环、连续内存读取和写回，传统流水线内自定义指令工具难以直接支持。Clay 在双端口存储接口下可以得到启动间隔为 2、总延迟约为 `2n+1` 的调度；在单端口存储接口下，由于读写资源冲突，启动间隔增加到 3、总延迟约为 `3n` @clay2025。同一 CADL 行为在不同微架构上产生不同调度结果，说明高层指令语义、接口资源和最终性能之间存在直接耦合。如果工具只把自定义指令看作一段待综合逻辑，而不把寄存器/访存端口、流水线阶段和响应延迟显式纳入约束，就很难得到可移植且质量可控的实现。

=== 硬件实现与接口生成

在硬件生成阶段，Clay 将调度后的 HIR 降低到 LIR 和 RuleIR。LIR 由已经排好周期的基本块组成，RuleIR 则进一步把每个周期或阶段中的行为组织成规则；当数据跨规则使用时，工具自动插入流水寄存器 @clay2025。对于包含多个基本块的状态化指令，Clay 根据控制流生成有限状态机，用状态 guard 控制规则触发，并用状态转换逻辑连接不同状态。这个过程把 CADL 中的高层控制流转化为明确的硬件状态和时序行为，使状态化自定义指令不再依赖设计者手工编写协处理器控制器。

不同耦合策略在这一阶段被具体化。对于流水线内耦合，规则绑定到目标处理器的流水线阶段，并由阶段范围约束保证译码、寄存器读、执行、访存和写回等动作落在合法位置。对于协处理器耦合，Clay 生成请求-响应接口、内部状态机和必要的数据通路；当响应延迟不可预测时，例如 RoCC 接口后接 cache 层次结构，生成逻辑采用延迟不敏感的停顿机制等待数据返回 @clay2025。最终实现通过 cmt2 生成 SystemVerilog。这一流程使 Clay 既能为简单指令生成轻量级流水线内扩展，也能为复杂状态化指令生成协处理器式实现。

=== 实验评估与意义

Clay 的评估覆盖两类 RISC-V 处理器：Clay-core 是 RV32I、五级全旁路、无 cache、读写通道分离的处理器，支持流水线内耦合和协处理器耦合；Rocket-core 是 RV32IMAC 五级处理器，带 cache 层次结构和 RoCC 协处理器接口，实验中主要使用协处理器耦合 @clay2025。评估的自定义指令包括 `simd`、`complex_mul`、`sbox`、`autoinc`、`crc`、`cordic`、`gemm2x2` 和 `stream_add`。除查表型 `sbox` 外，多数 CADL 描述不超过 30 行，RTL 生成时间低于 1 秒；这说明 Clay 的前端并不是以牺牲描述成本换取实现能力，而是把复杂接口和时序问题后移给综合器处理。

单指令实验展示了不同机制的作用。简单组合指令可以通过流水线内耦合获得低开销实现，例如 `simd` 在 Clay-core 上周期数下降 95%，对应 20.0x 加速；`complex_mul` 在 Clay-core 上获得 203.2x 加速，但也带来更长时钟周期，这说明高性能自定义算术逻辑需要同时考察周期数和时序代价 @clay2025。状态化指令则展示了 Clay 超出传统流水线内工具的能力：`cordic` 若完全展开会造成严重时序压力，而迭代式协处理器实现能够避免组合路径过长；`crc` 因位操作延迟较低，循环展开反而可能更合适；`stream_add` 的变长循环和多次访存也需要借助协处理器与调度约束处理 @clay2025。这些结果说明，耦合策略不能由固定规则决定，而应由指令行为和目标微架构共同决定。

端到端工作负载进一步验证了 Clay 的实用性。EdgeDet 将 rgba2gray、3x3 Sobel、3x3 erode 和 3x3 dilate 等图像处理操作定制为指令，4 条自定义指令共 87 行 CADL，优化后程序仅含 17 条指令，并在面积增加约 39% 且频率基本保持的情况下获得端到端收益 @clay2025。CFOEst 则定制 complex_mean、complex_mul 和 cordic，3 条自定义指令共 50 行 CADL，将动态指令数从 1113 条降至 9 条，并获得 33.9x 加速；其面积和时钟周期增加主要来自乘法器、CORDIC 数据通路和自定义状态 @clay2025。相较只报告单个 kernel 加速，这些工作负载结果更能说明 Clay 面向的是可落地的 ASIP 设计流程：设计者用较少前端代码描述复杂指令行为，工具根据目标微架构生成不同实现，并通过周期、时序和面积共同评价结果。

从本报告角度看，Clay 在第三章中承担连接第二章与第三章的关键角色。第二章强调应用需求和可复用指令/微架构能力如何通过架构接口被发现和表达；Clay 进一步回答，当一条能力已经成为自定义指令后，如何让它在不同处理器微架构上以合适的耦合策略、时序安排和资源使用落到硬件实现。它也与 Cement 形成清晰分工：Cement 解决一般硬件前端如何表达周期确定行为，Clay 解决 ASIP 指令扩展如何把微架构属性纳入综合约束。二者共同说明，高质量硬件实现不是后端黑盒综合的附属结果，而需要在前端抽象、接口原语、时序属性、资源约束和综合算法之间形成可检查的闭环。

== 变换、映射与调度联合优化

=== 研究背景与设计目标

前面三个小节分别从基础设施、前端时序表达和微架构感知 ASIP 综合角度讨论硬件实现质量。SkyEgg 则进一步指出，许多硬件综合质量问题并不是单个前端或调度器可以解决的，而来自代数变换、硬件映射和调度被人为拆成固定顺序的阶段。传统 HLS 通常先进行前端优化，再把操作调度到时钟周期，最后选择 LUT、DSP 或 IP core 等硬件实现；但调度需要知道硬件映射的精确延迟和资源行为，映射选择也依赖调度决定操作能否同周期串接，代数变换又可能改变可用映射模式。把这些阶段分开，会错过必须跨层联合选择才能发现的高质量实现 @skyegg2026。

SkyEgg 的目标是把“表达式如何变换、操作如何映射、映射如何调度”放到同一个设计空间中处理。它以 MLIR 程序为输入，构造初始等价图；随后从目标 FPGA 平台库生成映射重写规则，把软件操作和 LUT、DSP、浮点 IP 等硬件映射联系起来；等价饱和阶段同时应用一般代数重写规则和硬件映射规则，扩展出包含软件等价形式与硬件映射候选的饱和等价图；最后，SkyEgg 不再执行传统的低成本表达式提取，而是把提取改写为“变换-映射感知”的调度问题，用精确 ILP 或 ASAP 启发式求解，并生成 SystemVerilog 硬件实现 @skyegg2026。

#figure(
  image("assets/skyegg/overview.pdf", width: 78%),
  caption: [SkyEgg 源文系统概览。输入 MLIR 程序先进入等价图设计空间，平台映射库生成映射重写规则，饱和后通过 ILP 或 ASAP 调度得到硬件映射和时序安排。],
)

=== 等价图设计空间与硬件映射表示

SkyEgg 首先把 MLIR 静态单赋值形式中的每个值转换为等价图节点，节点构造子来自对应 MLIR 操作，节点参数指向操作数所在的等价类。以源文中的 `-(a+b)*c` 为例，初始等价图只表达软件计算结构，并不包含任何硬件实现信息。为了让等价图同时承载硬件映射空间，SkyEgg 定义了一类专门的 mapping e-node，形式为 `id(Vec(arg1, arg2, ...))`。其中 `id` 表示一个具体硬件映射，例如 DSP48E2 或 LUT；`Vec` 中的参数表示该硬件映射的输入 @skyegg2026。

硬件映射通过映射规则插入等价图。每条映射规则由三部分组成：matcher 描述硬件单元能够实现的软件代数模式；applier 在匹配位置插入相应 mapping e-node；condition 约束数据类型、位宽或硬件原语限制 @skyegg2026。以 DSP48E2 为例，它的输入 A、D、B 分别受到不同比特宽度限制，因此同一个代数表达式能否映射到 DSP，不仅取决于操作种类，还取决于数据类型和位宽。SkyEgg 把这些约束作为映射规则条件显式放入等价图扩展过程，使硬件原语的功能、配置和限制不再隐藏在后端绑定阶段。

#figure(
  image("assets/skyegg/egraph.pdf", width: 94%),
  caption: [SkyEgg 等价图表示示例。源文用代数重写规则和映射规则共同扩展设计空间，使软件等价形式和 DSP/LUT 等硬件映射候选在同一结构中出现。],
)

SkyEgg 还区分三类硬件映射配置。第一类是 basic logic，即用 Verilog 操作符描述、没有配置变体的 LUT 逻辑；第二类是 hardware primitive，例如 DSP48E2，其内部流水寄存器可以独立打开或旁路；第三类是 parameterized IP core，例如浮点 IP，其整体延迟和资源 preset 可以配置 @skyegg2026。对于每个硬件映射，SkyEgg 会枚举可用配置，并把配置参数编码进 mapping e-node 的 `id` 中。这样，后续调度面对的不是抽象的“乘法”或“DSP”，而是一个具有确定延迟、时序属性和资源行为的 mapping-configuration pair。

=== 时序模型与调度式提取

为了让映射选择真正影响调度，SkyEgg 为每个目标映射建立时序属性模型。一个映射包含延迟、输入延迟、输出延迟和寄存器到寄存器延迟等属性。对于组合映射，输入延迟描述输入端口到输出端口的组合路径；对于含流水寄存器的映射，输入延迟描述输入到第一级寄存器的路径，输出延迟描述末级寄存器到输出的路径，寄存器到寄存器延迟则约束内部流水线可达到的最小时钟周期 @skyegg2026。由于 FPGA 原语和 IP core 的供应商时序数据通常不公开，SkyEgg 预先用综合工具实例化每个映射及配置，施加时序约束并提取综合报告，从而建立时序属性数据库。该数据库不追求替代最终布局布线结果，但足以指导调度阶段的映射选择和同周期串接判断。

在等价图上进行调度还需要判断映射之间的结构路径延迟。若从一个映射到另一个映射之间只经过组合映射，则这条路径可能在同一时钟周期内串接；若路径延迟超过目标时钟周期，则必须插入流水寄存器切断组合路径。SkyEgg 源文将路径延迟定义为源映射输出延迟、中间映射输入延迟、目标映射输入延迟和连线延迟的累积，并用所需切分寄存器数量把时序闭合要求转化为调度约束 @skyegg2026。这个模型把“能否同周期串接”从 HLS 后端的隐式启发式，变成了等价图提取阶段可显式推理的约束。

在问题形式化上，SkyEgg 将传统等价图提取转化为变换-映射感知调度 ILP。决策变量包括等价类是否被选中、mapping e-node 是否被选中、mapping e-node 的开始/结束时间，以及等价类的完成时间。约束分为两类：完整性约束保证根等价类被计算、被选中的等价类至少选择一个 mapping e-node、被选中的 mapping e-node 的子等价类也被计算；调度约束保证依赖关系、延迟关系、等价类完成时间和时序串接约束成立 @skyegg2026。目标函数最小化根等价类完成时间，并用小权重惩罚项减少不必要映射选择。这个形式化的关键不是“用 ILP 替代提取”本身，而是在同一个优化问题中同时决定表达式形态、硬件映射和时钟周期安排。

=== 高效求解与实验评估

SkyEgg 的求解难点主要来自串接约束，因为饱和等价图中许多 mapping e-node pair 之间存在大量可行路径。若只保留最大路径延迟，最大路径上的中间 mapping e-node 可能最终没有被选中，实际被选路径反而没有得到约束；若保留全部路径，ILP 规模又难以扩展 @skyegg2026。SkyEgg 因此提出 top-k path delays，在每对 mapping e-node 之间保留若干最长路径作为约束，默认 `k=3` 即可使实验设计满足目标时钟周期。这个选择反映了 SkyEgg 的工程取舍：既不能把时序闭合简化为单一路径，也不能让完整路径集合压垮求解器。

为了进一步扩展问题规模，SkyEgg 还提出 ASAP 启发式调度器。它按拓扑顺序遍历等价类，为每个等价类选择最早完成的 mapping e-node，并在确定开始时间时同时考虑依赖约束和以该节点为终点的 top-k 串接约束 @skyegg2026。该调度器是启发式而非全局最优，因为当前等价类中“最早完成”的映射可能给后续等价类带来较长组合路径；但源文实验显示，它在多数情况下接近 ILP 解，同时把求解时间降低到适合大规模任务的范围。

#figure(
  image("assets/skyegg/speedup_ilp_vs_asap.pdf", width: 96%),
  caption: [SkyEgg 的 ILP 与 ASAP 调度性能对比。源文评估显示 ASAP 在大多数基准上接近 ILP，同时显著降低求解开销。],
)

实验上，SkyEgg 使用 HECTOR 将输入程序降低到 MLIR 并提取基本块，再将其转为 C++ kernel，基线为 Vitis HLS 2024.1，目标平台为 Xilinx xcku3p FPGA；SkyEgg 的 Verilog 输出使用 Vivado 2024.1 综合，ILP 求解使用 OR-Tools 并设置 1 小时超时 @skyegg2026。映射库覆盖整数和浮点操作：整数侧包含 LUT 逻辑、DSP48E2 和除法 IP，浮点侧使用 Xilinx floating-point IP core 并考虑不同流水配置。基准包含 liquid-dsp 数学 kernel、ggml 神经网络 kernel、PolyBench 科学计算 kernel、Vitis Library 信号处理 kernel，以及用于可扩展性分析的 100 到 600 操作合成基准。

结果显示，SkyEgg 的变换-映射感知调度相对 Vitis HLS 在全部基准和频率目标上平均获得 3.10x 加速，其中 ILP 平均 3.12x，ASAP 平均 3.08x；浮点基准平均 3.64x、最高 5.22x，整数基准平均 2.38x、最高 4.33x @skyegg2026。更重要的是，SkyEgg 的 ILP 和 ASAP 设计在所有频率目标下均满足时序约束，而 Vitis HLS 在高频目标下有 48% 的设计无法满足时序。资源方面，SkyEgg 的 FF 使用与 Vitis HLS 相近，LUT 使用有所增加，体现出以适度逻辑复杂度换取延迟和时序收益的取舍 @skyegg2026。可扩展性实验中，ASAP 在 100 到 600 操作的整数和浮点合成基准上均能在 1 秒内完成，而 ILP 在大规模浮点问题上频繁超时；这说明 SkyEgg 同时提供了精确优化路径和可扩展工程路径。

=== 本节小结：从前端表达到联合优化

SkyEgg 是本章的方法高潮，因为它把前三节强调的几个判断合到同一个优化框架中。HECTOR 说明硬件综合需要 IR 化和多层方法学支撑；Cement 说明时序行为必须显式表达并可检查；Clay 说明微架构属性和耦合策略必须进入综合约束；SkyEgg 则说明一旦这些事实进入 IR 和设计空间，工具还需要避免固定 pass 顺序和阶段割裂，把变换、映射与调度作为联合问题求解。由此，本章从“如何表达硬件”推进到“如何在语义等价空间和硬件映射空间中选择高质量实现”。

这一点也自然引出第四章。SkyEgg 仍主要依赖预定义重写规则、硬件映射候选、ILP/启发式调度器和工程化代价形式化；随着设计空间变大，哪些规则应当被应用、哪些策略能在不同任务上复用、如何把专家经验和历史证明转化为可审计策略，成为新的问题。第四章中的 EggMind 正是在 SkyEgg/ISAMORE 所代表的等价图优化基础上，把大模型的策略生成能力纳入形式化可控的 EqSat 策略对象中，进一步讨论大模型与形式化技术如何共同服务软硬件协同。

== 本章小结

本章回应第一章提出的第二个科学问题：架构能力如何高质量落到可综合、可验证、性能与资源可控的硬件实现。HECTOR 提供了基于 MLIR 多层 IR 的早期硬件综合基础设施视角，说明硬件实现质量需要结构化 IR 和 lowering 流程支撑。Cement 强调前端必须显式表达周期行为，并通过周期分析与有限状态机综合保证硬件行为可预测、可检查、可综合优化。Clay 将这一问题带回 ASIP/ISAX 场景，强调耦合策略、微架构属性和综合约束对定制指令实现质量的决定性作用。SkyEgg 则进一步把代数变换、操作映射和调度统一到等价图设计空间中，避免固定阶段顺序造成的次优实现。

这些工作共同说明，高质量硬件实现不是后端工具的附属结果，而需要从前端表达、IR 设计、时序语义、微架构属性、映射选择和调度求解共同组织。它们也为下一章奠定了方法基础：当等价图、MLIR、硬件 IR 和综合反馈成为软硬件协同的核心对象后，大模型和智能体方法若要进入这一流程，就必须操作这些结构化对象，并受到形式化语义、验证检查和执行证据约束。
